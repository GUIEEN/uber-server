type Verification {
  id: Int!
  target: String! # email or password or token etc..
  payload: String! # key
  key: String!
  used: Boolean!

  user: User

  createdAt: String!
  updatedAt: String!
}
